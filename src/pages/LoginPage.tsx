import { MButton, MCard } from '@/components'

export default function LoginPage() {
  return (
    <MCard className="mx-auto text-center">
      <h1 className="mb-4 text-3xl font-bold">Log In</h1>
    </MCard>
  )
}
